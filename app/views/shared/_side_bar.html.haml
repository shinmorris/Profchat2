.side-bar
  .side-bar__user-name
    .top-items
      %p.top-items__name
        = current_user.name
      %ul.top-items__icons
        %li
          =link_to new_group_path do
            =icon('fas', 'edit', class: 'icon')
        %li
          =link_to edit_user_path(current_user) do
            =icon('fas','fas fa-cog',class: 'icon')
  .side-bar__group-list
    - current_user.groups.each do |group|
      .group-box
        = link_to '#' do
          %p.group-box__group-name
            = group.name
          %p.group-box__group-message
            メッセージはまだありません。
